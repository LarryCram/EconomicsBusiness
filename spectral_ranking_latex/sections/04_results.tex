%!TeX root = ../main.tex
% ---------------------------------------------------------------
\section{Results}
\label{sec:results}
% ---------------------------------------------------------------


A glossary of the terms used in the model is exhibited in Table~\ref{tab:parameters}. There are three structural arrangements: (1) blocks, picked by the unit mask $m$ and encoded as $1000 \rightarrow S$ (sources only), $0001 \rightarrow I$ (institutions only), $0110 \rightarrow B$ (bipartite) and $1111 \rightarrow J$ (joint), (2) subfields, picked from $\mathfrak{F}$ (E, B, A, X), and (3) census and target time windows, overlapped ($t_c=t_t$) and picked from the sets \[\{T_t: t = 1 \ldots 5\} = \{[2000, 2004], [2005, 2009], [2010, 2014], [2015,2019], [2020,2024]\}.\] These fully overlapping windows accumulate relatively more references to recent works. We also present a sensitivity analysis for the parameters $\rho,~\chi,~\omega,~\epsilon,~(\alpha,~\mu),~\rm{and}~ t_c$, together with a bootstrap study of uncertainties. Results focus on $\mathbf{v}_S$ and $\mathbf{v}_I$, the source and institution influence vectors, derived from the ranking probability by Equation \ref{eq:vstar}. An instance of the model is designated using a subscript for the unit and superscripts as required to designate structural arrangements and parameters. For example, $\mathbf{v}_I^{S,E,T_5}$ is the institution influence vector for the $S$ ($m=1000$) block, $F=E$ (Economics), and census and target time windows 2020-2024. Throughout this work, logarithms are base 10.

\input{../tables/table_parameters}

Figure~\ref{fig:fig_3by} compares influence values across the four unit masks. The $x$-direction is the cardinal rank of sources (left) and institutions (right) in the baseline (i.e. bipartite resolvent with $S \rightarrow I \rightarrow S,~\alpha=1,~\mu=0,~\rho=0,~\varepsilon=0,~\omega=0,~T_5 = [2020-2024]$ and $F=EBAX,~\rm{all~subfields}$). The solid black curve traces the corresponding influence value $v$ in the $y$-direction. Units near $v=1$ have average influence, corresponding to rank position $\approx~15\%$ for sources and $\approx~25\%$ for institutions. For most sources and institutions, $v$ lies between  1 and 0.1. Overlaid on the bipartite source curve are $v_S^S$ (red $\times$, $S \rightarrow S$) and $v_S^J$ (green $\times$, joint $S+I \rightarrow S+I$). A point on the $x$-axis is the same unit in every case. For institutions, we show $v_I^I$ ($I \rightarrow I$) and $v_I^J$ (joint). Figure~\ref{fig:fig_3bz} is another view of these results, directly comparing bipartite $v^B$ and single-unit $v^{S,I}$ influence, colouring points by subfield (see below).

\begin{figure}[!hbt]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_3by_latex.pdf}
  \caption{Logarithm of influence versus rank for 
    sources (left) and institutions (right).
    Black line: $v^B$ (bipartite, baseline);
    red circles: $v_S^S$ / $v_I^I$ (within-block);
    green diamonds: $v^J$ (joint $S+I$).
    $x$-axis is the cardinal rank for the baseline.}
  \label{fig:fig_3by}
\end{figure}

Notable features are the small proportion of high-$v$ units, the tightness of the institution dispersion compared with sources, and the difference between the source influence in the bipartite baseline $v_S^B$ and the source-only case $v_S^S$. These features arise from the structure of the citation matrices $\mathbf{H}$ and $\mathbf{M}$, in particular the way that references spread as they are given, and concentrate as they are received. To characterise this reference flow we consider the Shannon evenness $J$ \citep{stirlingGeneralFrameworkAnalysing2007}. For an $n_{\text{row}}\times n_{\text{col}}$ row stochastic citation matrix $X~=~x_{ij}$, $J$ over row $i$ is defined as 
\begin{equation}
  J_i = \frac{x_i}{\log n_{\text{col}}}, \qquad
  x_i = -\sum_j X_{ij}\log X_{ij},
  \label{eq:row_evenness}
\end{equation}
with $J_i\in[0,1]$. If a row distributes references uniformly $J=1$, while concentration of references to one column gives $J=0$. The mean over all rows,
\begin{equation}
  \bar{J} = \frac{1}{n_{\text{row}}}\sum_i J_i,
  \label{eq:J_bar}
\end{equation}
is relatively large when outward references are dispersed evenly across the whole corpus. The fan-out $\bar{K} = n_{\text{col}}^{\bar{J}}$ converts normalised row evenness to a count: the geometric mean number of distinct targets reached in one step over the Markov matrix.
A similar measure $J_{\text{col}}$ applies to the mean column sums $p_j = \sum_iX_{ij} / n_{\text{row}}$. Because X is row-stochastic, $p_j$ is a valid marginal distribution and
\begin{equation}
  J_{\text{col}} = \frac{-\sum_j p_j \log p_j}{\log n_{\text{col}}},
  \label{eq:J_col}
\end{equation}
measures its variability, with $J_{\text{col}}=1$ when units receive equal reference in-flow and $J_{\text{col}}\to 0$ when references flow only to a few units. Note that $p_j$ is a version of the journal impact factor (JIF). 


\input{../tables/table_kernel_structure}


Table~\ref{tab:kernel_structure} quantifies how, in one step, institutions spread outgoing references more broadly than sources ($\bar{J}_{II}=0.73$ vs $\bar{J}_{SS}=0.47$; fan-out $7.9\times$ larger), while receiving incoming references only slightly more broadly than sources ($J_{col, II}=0.95$ vs $J_{col, SS}=0.90$). In two-step kernels, $\mathbf{M}_S$ spreads references more evenly than $C_{SS}$ ($\bar{J}:$ 0.81 c.f. 0.47) while also narrowing the spread of incoming references ($J_{\text{col}}$: 0.83 c.f. 0.90). The bipartite round-trip spreads attention/influence broadly while rewarding the most-cited sources, raising $v$ for high-ranked sources as shown in Figs~\ref{fig:fig_3by} and \ref{fig:fig_3bz}: normalisation of influence then reduces $v$ for lower ranked sources. For institutions, $\mathbf{M}_I$ is more balanced ($\bar{J}=0.90$, $J_{\text{col}}=0.93$): bipartite paths make little difference compared with institution-only paths, although $v_I^I$ is slightly less than $v_I^B$ for the highest ranks. The joint resolvent covering the full citation matrix yields v between the bipartite and single-unit value.

\begin{figure}[!htb]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_3bz_latex.pdf}
  \caption{Bipartite vs within-layer influence for the full corpus: economics and business separate even when ranked together.
    Sources (left): $\log v_S^S$ vs $\log v_S^B$.
    Institutions (right): $\log v_I^I$ vs $\log v_I^B$.
    Points coloured by subfield (E, B, A, X).}
  \label{fig:fig_3bz}
\end{figure}

With this understanding of the role of unit masking, we now explore the effects of subfields. Figure~\ref{fig:fig_3bz} shows that subfields stand out in unit influence values, \textit{even when subfields are not used in the ranking}. The prominent effect is the differential response of $E$ and $B$ sources to the bipartite ranking compared with the single unit ranking. Specifically, $E$-sources receive an influence boost due to greater unit concentration as described above, compared with $B$-sources. The separation exemplifies the observation of \cite{pinskiCitationInfluenceJournal1976} and \cite{waltmanSourceNormalizedIndicators2013} that spectral ranking is sensitive to the presence of subfields: not only are there different responses, but these responses may themselves alter the rankings and influence scores.  

Figure~\ref{fig:fig_2c} illustrates the process by exhibiting influence distribution when unit sets are truncated to lie within the $E$ or $B$ subfields. Rather than the subfield separation seen in Figure~\ref{fig:fig_3by}, subfields are mingled over similar distributions. Both $B$ and $E$ sources attain similar values of high influence, with influence for $B$-sources rising by around 10-20\% and $E$-sources falling by a comparable amount compared with ranking over the combined subfields. The different behaviour between combined and separated subfields is apparent in the second eigenvector of the combined corpus (not shown), where the two subfields are cleanly separated by sign. Subfield separation occurs despite the relatively low modularity of the full corpus, 0.2 for the source-only network, 0.07 for institutions, and 0.002/0.0001 for the two forms ($S \rightarrow I$ and $I \rightarrow S$) of the bipartite network.

\begin{figure}[!htb]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_2c_latex.pdf}
  \caption{Bipartite vs within-layer influence, evaluated separately for each subfield.
    Sources (left): $\log v_S^S$ vs $\log v_S^B$.
    Institutions (right): $\log v_I^I$ vs $\log v_I^B$.
    Points coloured by subfield (E, B, A, X).}
  \label{fig:fig_2c}
\end{figure}

Figure~\ref{fig:fig6} plots $v$ for all sources and institutions across
the five time windows $t_x=1,\ldots,5$ (2000--2024), with the $x$-axis
locked to the baseline rank. The trend of the running means reveals an influence hierarchy that broadly persists over 25 years.  
The Spearman correlation between $v$ in 2000--04 and $v$ in 2020--24 is approximately $\rho=0.7$ for both sources and institutions in the common sets. The consecutive-window values are higher, implying that the scatter in Figure~\ref{fig:fig6} arises from long-run drift rather than short-run
volatility. Institutions are no more stable in rank than sources, despite having
more volatile absolute $v$ values across windows. The volatility reflects the
growing size of the institution corpus as additional universities
cross the $\tau_I$ threshold over the 25-year period.
The greater smoothness of the institution panel in Figure~\ref{fig:fig_3by}
and the comparable long-run rank stability in Figure~\ref{fig:fig6}
point to the same mechanism: the $SI$--$IS$ coupling acts as a
stabiliser, averaging out fluctuations in citation practice
that would otherwise produce volatile source rankings.

\begin{figure}[!hbtp]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_6_latex.pdf}
  \caption{Influence $v$ against baseline rank across five
    time windows.
    Black line: baseline $t_x=5$ (2020--24).
    Coloured series: $t_x=1$ (2000--04) through $t_x=4$ (2015--19).
    $x$-axis locked to baseline rank; units absent from the window
    are omitted from that series.}
  \label{fig:fig6}
\end{figure}

Figure~\ref{fig:figstability} quantifies the achievement and persistence of
above-average influence - defined as $v>1$ - across the 25-year span.
Approximately 15\% of sources and 27\% of institutions have
above-average influence in each window, and this fraction is stable over time.
Considering the 651~sources and 417~institutions present in both the
2000--04 and 2020--24 windows, $86\%$ of sources and $96\%$ of
institutions with above-average influence in 2000--04 retain that status
in 2020--24. The scatter plots exhibit the asymmetry: very few sources or institutions move from 
$v>1$ in 2000-04 to $v<1$ in 2020-24 (bottom right quadrant), and many move from $v<1$ to $v>1$
(upper left). Institutions that crossed the $v=1$ threshold between 2000--04 and 2020--24
are predominantly universities in Global North countries that expanded their economics and business research output over the period, consistent with the growth of internationally oriented business schools
and the associated increase in research-active faculty size. 

\begin{figure}[!tbp]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_stability_scatter_latex.pdf}
  \caption{Influence $v$ in 2000--04 against $v$ in 2020--24
    for units present in both windows (log--log) with baseline parameters.
    Left: sources ($n=651$).  Right: institutions ($n=417$).
    Diagonal: $v_{t_1}=v_{t_5}$. Hue encodes threshold-crossing status, e.g. red denotes the few units that decline across $v=1$ between 2000 and 2024.}
  \label{fig:figstability}
\end{figure}

In addition to the structural results presented above (units, subfields and time windows), the model includes several parameters whose choice, while arbitrary, alters the influence values.  Figure~\ref{fig:fig5} shows influence $v$ against baseline rank for five parameter variants, overlaid on the baseline. 

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_5_latex.pdf}
  \caption{Parameter sensitivity of influence $v$ against baseline
    rank.
    Left: sources.  Right: institutions.
    Black line: baseline ($\tau_I{=}10$, $\tau_S{=}5$, $\rho{=}0$, $\alpha{=}1$, $\varepsilon{=}0$).
    Red $\times$: $\tau{=}40$ ($N_S{=}663$, $N_I{=}1{,}125$).
    Blue $\times$: $\rho{=}1$.
    Green $\times$: census year 2024 only.
    Yellow $\times$: $\omega=1$.
    Purple $\times$: $\varepsilon=1$.
    $x$-axis locked to baseline rank; units absent from a variant are omitted.}
  \label{fig:fig5}
\end{figure}

Raising the work-per-year threshold to $\tau_I{=}\tau_S{=}40$ reduces the corpus size to around 40\% for sources and 57\% for institutions. Despite this reduction, the influence scores of the surviving units track the baseline closely, with Spearman coefficients $>~0.97$. However, the higher threshold removes low-ranked units differentially, raising influence for the most influential units and lowering it by about $0.1$ for sources and $0.06$ in $\log{v}$ for the bulk of units. Consequently, the median absolute rank shift is large among the lower-influence population, 7.5\% for sources and 6.4\% for institutions that remain in the corpus. 

Removing the normalisation of reference list lengths ($\rho{=}1$)
leads to a small overall reduction of average source influence, -0.07 in $|\log(v_{\rho}/v)|$, while introducing substantial changes in individual influence values, s.d. $\log{r} = 0.2$ for sources and s.d. $\log{r} = 0.1$ for institutions. These relatively large fractional variations occur across all units, leading to a median change in rank of 5\% for sources and institutions. 

The overlapping census and target windows for the baseline model `over-count' references that are close in publication date. When the census window is reduced to one year ($t_c=2024$), total reference counts reduce and many values of $v$ change  (s.d. $\log(r) = 0.2 (\rm{sources}), 0.1 (\rm{institutions})$). Because the reduction in citer-cited pairs occurs uniformly over $\mathbf{H}$ the shape of $v^{S,I}$ is similar to $v^B$.

Moving between institution weights based on author fractional counting and inform counting leads to very small changes in source $v$; for institutions $s.d. \log{r} = 0.04$ with no change in median value, and the median rank changes by less than 1.6\%.

When the baseline is modified by including the sentinel units ($\varepsilon=1$) the presence of the single large source and sink for all corpus units reduces the normalised size of within-corpus exchanges, flattening and shuffling the distribution of $v$ for sources and institution. The median $|\log(v_{\varepsilon}/v)|$ is 0.3 for sources and 0.25 for institutions, and the median rank shift is around 10\%. We emphasise that the use of single sentinel units is only a coarse approximation to the flow of references to and from the corpus.

Commonly, bibliometric spectral rankings adopt the Katz-Hubbell (PageRank) algorithm, with an attenuation \citep{katzNewStatusIndex1953} $1-\alpha>0$ and a boundary condition \citep{hubbellInputoutputApproachClique1965} $\mathbf{\mu} \ne \mathbf{0}$ \citep{bollenJournalStatus2006, bergstromEigenfactorMetricsFigure2008}. With $\alpha=0.85$ and $\mu_p=1/N_p$, the baseline model is radically altered as shown in Figure~\ref{fig:fig5d}. Units that had low $v$ in the Perron-Frobenius model are held up by the floor $(1-\alpha)\mathbf{\mu}$ on the Katz-Hubbell eigenvector $\mathbf{\pi}$. Because influence $v_p \propto \pi_p/a_p$, units with only few works $a_p$ may amplify this lift, leading to the occasional large rises seen in Figure~\ref{fig:fig5d}. Comparison between Figures~\ref{fig:fig5d} and \ref{fig:fig_3by} illustrates the straightforward interplay between the block models and the Katz-Hubbell algorithm: in the recirculating bipartite case, routing through units tends to spread influence compared with single-unit influence, although both are elevated by the boundary value $\mathbf{\mu}$. As noted above, we might choose $\mu = 1/(2N_p), p \subset \mathfrak{P}$ or $\mu=1/(N_S+N_I)$. Unsurprisingly, with $N_S \approx N_I$ this choice makes little  difference, less than 0.02 in the log. While similar to the $\alpha=0.85$ model, the Eigenvector ranking \cite{bergstromEigenfactorMetricsFigure2008}, excludes unit self-edges. We find that removing self edges systematically lowers $v$ when $v < 1$, by around 0.04 for sources and institutions, while the variation around this shift is 0.08 (s.d., sources) and 0.07 (s.d., institutions). For this data set, unit self-referencing contributes little to influence values or order.  

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_5d_latex.pdf}
  \caption{Comparing the Perron--Frobenius and Katz--Hubbell spectral rankings, with $\alpha=0.85$
  and the uniform prior $\boldsymbol{\mu}$.     Left: sources.  Right: institutions. The red symbols are the corresponding single-unit Katz-Hubbell influence values.
  The $x$-axis is locked to baseline  (Perron--Frobenius) rank; units absent from a variant are omitted.}
  \label{fig:fig5d}
\end{figure}

We now consider how uncertainties and errors in OpenAlex metadata propagate into in the uncertainty of a spectral ranking. The observed sources and institutions are drawn from a larger population, so sampling error arises; we model it using non-parametric bootstrapping
\citep{efronBootstrapMethodsAnother1992}. In addition, like all bibliometric databases,
OpenAlex includes a variety of metadata errors. We construct simple models of four potentially potent errors and apply stochastic simulation to propagate their 
effects into the spectral ranking. The errors are introduced at the work level and
carried through the full non-linear influence pipeline.

Table~\ref{tab:oa_errors} summarises the error types and their adopted rates.
Publication year error rates were estimated from the frequency of gaps in the concordance between recorded
publication year and the (Volume, Issue, Page) sequence for the $\approx$50\% of sources that have sequential
metadata. Source misallocation, while rare, is observed in practice (e.g.\ some works from
\textit{Soshiki Kagaku} are attributed to \textit{Organization Science}).
Institution attribution errors are informed by an unpublished study at TU Wien, which
estimates the error level at around 0.5\%; having found several hundred
works misattributed between Brown University and John Brown University we adopt a higher value. Referencing errors arise when reference lists are incorrectly transcribed, truncated, intermixed, or mis-indexed; the adopted error is informed by our experience and the report of \cite{rodriguesDetectingIncoherentCitation2025}.

\begin{table}[htbp]
  \centering
  \small
  \caption{Models of errors used in the stochastic simulation of spectral ranking uncertainty.
           Errors are introduced at the work level and propagate through the pipeline using the baseline parameters. The rate is the proportion of the set (works or references) in question that is randomly affected in a replicate.}
  \label{tab:oa_errors}
  \begin{tabular}{lp{9cm}r}
    \toprule
    Error model       & Mechanism & Rate \\
    \midrule
    Sampling
      & Works drawn with replacement from the baseline set.
      & --- \\
    Publication year
      & Random work from the time window [2016--2019] is inserted into its source
        with the publication year shifted into the census window.
      & 2\% \\
    Source mis-attribution
      & Corpus work reallocated to a randomly chosen corpus source.
      & 0.03\% \\
    Institution mis-attribution
      & Corpus work reallocated to a randomly chosen corpus institution.
      & 1\% \\
    Reference mis-attribution
      & Corpus reference redirected to another work within the corpus
        (75\% within the same source; 25\% to another source)
      & 2\% \\
    \bottomrule
  \end{tabular}
\end{table}

The stochastic simulation first constructs 1000 replicates of the corpus works adjusted for each kind of work-level error: sampling, publication year, and source/institution allocation. These are saved as differences from the baseline work set (Stage 1). It also constructs 1000 replicates of the work-level citer-cited table adjusted for reference list errors, saving differences from the baseline (Stage 2). Combining one Stage 1 replicate (work-level errors) with one Stage 2 replicate (reference errors) at a time, the algorithm applies the standard preprocessing and resolvent pipelines to generate 1000 replicates of the influence $\mathbf{v}$, and $\approx$ 3.5 million unit-level replicates. To avoid the need to model author uncertainties, we adopt institutional fractionation to determine a work's institution weights.  

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_7a_oa_errors_latex.pdf}
  \caption{Stochastic simulation of 1000 replicates of the influence vector, subject to OpenAlex uncertainties and errors on the 
  spectral ranking of the baseline model. The black line is the baseline and the red line the replicate median showing only minor deviation. The replicates of each unit are plotted as grey points, with a blue dot on the upper and lower extremes, showing that extreme variations occasionally arise among the $\approx$ 3.5 million unit replicates presented here.}
  \label{fig:fig_7}
\end{figure}

Figure~\ref{fig:fig_7} summarises the 1000 replicates, highlighting the large deviations in $v$ and thus rank that occasionally arise. Small units have less capacity to dilute a work-level error and the smallest quintile of sources is around ten times more susceptible than the largest. Considering the overall population of sources subject to the error model, $\approx$ 7.7\% have an influence value with a twofold or more increase or decrease relative to baseline, with increases outnumbering decreases 5:1. The change often due to alteration of the publication year of a well-referenced work, inserting it into a source that has relatively few works and raising its prominence in the citation matrix. Less common but also responsible for large increases is the reallocation of a reference from the source's works to a different work. In some replicates a source has a large decrement, generally due to the loss of a well-referenced work from a source with relatively few works. Institutions are notably less susceptible, with $\approx$ 0.5\% having a twofold or more change in $v$, with around 3:1 downward. Most of these changes are due to deleting corpus works that referenced the institution's works.

Our model of OpenAlex errors rests on plausible assumptions, not measured properties. The stochastic simulation functions as a sensitivity analysis, showing the implications for spectral rankings if the errors occur in this way and at this rate. The power-law distribution of citation frequencies for works, combined with units that just pass the volume threshold, leads inevitably to large but rare changes in influence of a unit in the presence of bibliometric metadata errors. Our experience in reviewing the influence values of units is consistent with this: surprising placements can be explained by identifying an error or ambiguity in the metadata. This finding from our simulation is consistent with the empirical study presented by \citep{scheidstegerHowSimilarAre2025} who found low agreement between unit-level indicators derived from 
different large bibliometric databases including OpenAlex. 