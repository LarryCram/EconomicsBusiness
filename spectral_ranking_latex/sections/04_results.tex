%!TeX root = ../main.tex
% ---------------------------------------------------------------
\section{Results}
\label{sec:results}
% ---------------------------------------------------------------


A glossary of the terms used in the model is exhibited in Table~\ref{tab:parameters}. There are three structural arrangements: (1) blocks, picked by the unit mask $m$ and encoded as $1000 \rightarrow S$ (sources only), $0001 \rightarrow I$ (institutions only), $0110 \rightarrow B$ (bipartite) and $1111 \rightarrow J$ (joint), (2) subfields, picked from $\mathfrak{F}$ ($E, B, A, X$), and (3) census and target time windows, overlapped ($t_c=t_t$) and picked from the sets \[\{T_t: t = 1 \ldots 5\} = \{[2000, 2004], [2005, 2009], [2010, 2014], [2015,2019], [2020,2024]\}.\] These fully overlapping windows `overcount' references to recent works in each window. We also present a sensitivity analysis for the parameters $\rho,~\chi,~\omega,~\epsilon,~(\alpha,~\mu),~\rm{and}~t_c$, a bootstrap study of work-induced ranking instability, and a Monte Carlo study of metadata errors. Results focus on $\mathbf{v}_S$ and $\mathbf{v}_I$, the source and institution influence vectors, derived from the ranking probability by Equation \ref{eq:vstar}. An instance of the model is designated using a subscript for the unit and superscripts as required to designate structural arrangements and parameters. For example, $\mathbf{v}_I^{S,E,T_5}$ is the institution influence vector for the $S$ ($m=1000$) block, $F=E$ (Economics), and census and target time windows 2020-2024. Throughout this work, logarithms are base 10.

Figure~\ref{fig:fig_3by} compares influence values across the four unit masks. The $x$-direction is the ordinal rank of sources (left) and institutions (right) in the baseline (i.e. bipartite resolvent with $S \rightarrow I \rightarrow S,~\varepsilon=0,~\rho=0,~\omega=0,~\chi=0.5,~\alpha=1,~\mu=0,~T_5 = [2020-2024]$ and $F=EBAX,~\rm{all~subfields}$). The solid black curve traces the corresponding baseline influence value $v$ in the $y$-direction. Units near $v=1$ lie near the work-count weighted average influence per work, corresponding to rank position $\approx$15\% for sources and $\approx$25\% for institutions. For most sources and institutions, $v$ lies between  1 and 0.1. Overlaid on the bipartite source curve are $v_S^S$ (red: $S \rightarrow S$) and $v_S^J$ (green: joint $S+I \rightarrow S+I$). A point on the $x$-axis is the same unit in every case. For institutions, we show $v_I^I$ ($I \rightarrow I$) and $v_I^J$ (joint). 

\begin{figure}[!hbt]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_3by_latex.pdf}
  \caption{Logarithm of influence versus baseline ordinal rank for 
    sources (left) and institutions (right).
    Black line: $v^B$ (bipartite, baseline);
    red: $v_S^S$/$v_I^I$ (within-block);
    green: $v_S^J/v_I^J$ (joint $S+I$).}
  \label{fig:fig_3by}
\end{figure}

Notable features are the small proportion of high-$v$ units, the tightness of the institution dispersion compared with sources, and the difference between the source influence in the bipartite baseline $v_S^B$ and the source-only case $v_S^S$. These features arise from the structure of the citation matrices $\mathbf{H}$ and $\mathbf{M}$, in particular the way that references spread as they are given, and concentrate as they are received. To characterise this reference flow we consider the Shannon evenness $J$ \citep{stirlingGeneralFrameworkAnalysing2007}. For an $n_{\text{row}}\times n_{\text{col}}$ row stochastic citation matrix $X~=~x_{ij}$, $J$ over row $i$ is defined as 
\begin{equation}
  J_i = \frac{x_i}{\log n_{\text{col}}}, \qquad
  x_i = -\sum_j X_{ij}\log X_{ij},
  \label{eq:row_evenness}
\end{equation}
with $J_i\in[0,1]$. If a row distributes references uniformly $J=1$, while concentration of references to one column over the row gives $J=0$. The mean over all rows,
\begin{equation}
  \bar{J} = \frac{1}{n_{\text{row}}}\sum_i J_i,
  \label{eq:J_bar}
\end{equation}
is relatively large when outward references are dispersed evenly across the whole corpus. The fan-out $\bar{K} = (n_{\text{col}})^{\bar{J}}$ converts normalised row evenness to a count: the geometric mean number of effective targets reached in one step over the Markov matrix.
A similar measure $J_{\text{col}}$ applies to the mean column sums $p_j = \sum_iX_{ij} / n_{\text{row}}$. Because X is row-stochastic, $p_j$ is a valid marginal distribution and
\begin{equation}
  J_{\text{col}} = \frac{-\sum_j p_j \log p_j}{\log n_{\text{col}}},
  \label{eq:J_col}
\end{equation}
measures its variability, with $J_{\text{col}}=1$ when units receive equal reference in-flow and $J_{\text{col}}\to 0$ when references flow only to a few units. Note that $p_j$ is a version of the journal impact factor (JIF). 

\begin{table}[htbp]
\centering
\caption{Shannon evenness and fan-out for the six citation kernels and the baseline model.
  $\bar{J}$: mean row evenness; $J_{\text{col}}$: column evenness of the
  marginal distribution; $\bar{K} = (n_{\text{col}})^{\bar{J}}$: geometric
  mean fan-out (distinct targets per step).}
\label{tab:kernel_structure}
\begin{tabular}{lccc}
\toprule
Kernel & $\bar{J}$ & $J_{\text{col}}$ & $\bar{K}$ \\
\midrule
$C_{SS}$ & 0.47 & 0.90 & 32 \\
$C_{II}$ & 0.73 & 0.95 & 256 \\
$C_{SI}$ & 0.62 & 0.94 & 114 \\
$C_{IS}$ & 0.62 & 0.84 & 97 \\
\midrule
$M_S$ & 0.81 & 0.84 & 389 \\
\midrule
$M_I$ & 0.91 & 0.93 & 960 \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:kernel_structure} quantifies how, in one step, institutions spread outgoing references more broadly than sources ($\bar{J}_{II}=0.73$ vs $\bar{J}_{SS}=0.47$; fan-out $7.9\times$ larger), while receiving incoming references only slightly more broadly than sources ($J_{col, II}=0.95$ vs $J_{col, SS}=0.90$). In two-step kernels, $\mathbf{M}_S$ spreads references more evenly than $C_{SS}$ ($\bar{J}:$ 0.81 c.f. 0.47) while also narrowing the spread of incoming references ($J_{\text{col}}$: 0.83 c.f. 0.90). The bipartite round-trip spreads attention/influence broadly while rewarding the most-cited sources, raising $v$ for high-ranked sources as shown in Figs~\ref{fig:fig_3by}: normalisation of influence then reduces $v$ for lower ranked sources. For institutions, $\mathbf{M}_I$ is more balanced ($\bar{J}=0.90$, $J_{\text{col}}=0.93$): bipartite paths make little difference compared with institution-only paths, although $v_I^I$ is slightly less than $v_I^B$ for the highest ranks. The joint block model covering the full citation matrix yields $v$ between the bipartite and single-unit value.

\subsection{Business and Economics}

With this understanding of the role of unit masking, we now explore the effects of subfields. For clarity, in this subsection we restrict consideration to $E$ and $B$ units only (no $A$ or $X$). To focus on the effects of subfields alone, we first set aside bipartite ranking. Figure~\ref{fig:combined_vs_separate} compares source-only ($m=1000$) and institution-only ($m=0001$)cardinal rankings for both fields, $E+B$ ($x$-axis), with separate subfields, $E$-only and $B$-only ($y$-axis). 

First consider sources, Figure~\ref{fig:combined_vs_separate} (left). The linearity (in log--log) indicates that the ordinal ranking among $E$ and $B$ sources is not strongly effected by ranking together or separately. The explanation of the separation between $E$ and $B$ lies in the role of $\approx$10 top $E$ sources that have $v>10$ in the joint ranking, illustrated by the red circle.
\begin{figure}[!htb]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_3eb_latex.pdf}
  \caption{Cardinal rankings for separate ($E||B$) and combined ($E+B$) subfields, using the source-only (m=1000) block (left) and institution-only block (right). The $x$-axis is the combined ranking, while the $y$-axis is the ranking computed separately for each field ($E||B$). The red circle delineates the \textit{clique} of top $E$ journals.} 
  \label{fig:combined_vs_separate}
\end{figure}

Define the directed density $d_{XY}$ ($X, Y \in \{E, B\}$) as the per-directed-pair average of citation matrix elements (with $\rho=0$) from group $X$ to group $Y$, with denominator $N_X(N_X-1)$ for within-group pairs and $N_X N_Y$ for cross-group pairs. As exhibited in Table~\ref{tab:link_density}, the top-ten $E$ sources are mutually linked at a rate $49.61/0.272=182$ times the density among other $E$ sources, and $49.61/0.307=161$ times the density among all $B$ sources. They also receive $3.20/0.728=4.4$ times more references from top $B$ sources than they give to them. Spectral ranking over the combined $E+B$ corpus drives the total influence probability of this \textit{clique} of 10 $E$ sources to 25.9\%. For $E$-only ranking the influence probability of the clique rises to 38.5\% since the $E$ network is smaller. This increase reduces the influence probability and cardinal ranking of other $E$ sources and flattening the slope of the red $E$ points in the left-hand panel of Figure~\ref{fig:combined_vs_separate}. The influence of the clique is not present in the green points ($B$ only), so their ordinal rankings rise. 
\begin{table}[!htbp]
\centering
\caption{Directed link density $d_{XY}$ (average citation weight per directed pair) between source communities. Rows = citing group; columns = cited group. `Top' means top-ten sources by field-specific $SS$ ranking.}
\label{tab:link_density}
\begin{tabular}{lrrrrrr}
\toprule
 & \makebox[1.6cm][r]{\small Top-$E$} & \makebox[1.6cm][r]{\small Rest-$E$} & \makebox[1.6cm][r]{\small All-$E$} & \makebox[1.6cm][r]{\small Top-$B$} & \makebox[1.6cm][r]{\small Rest-$B$} & \makebox[1.6cm][r]{\small All-$B$} \\
\midrule
Top-$E$ & 49.61 & 0.535 & 1.11 & 0.728 & 0.028 & 0.035 \\
Rest-$E$ & 3.70 & 0.272 & 0.316 & 0.179 & 0.069 & 0.070 \\
All-$E$ & 4.23 & 0.275 & 0.327 & 0.186 & 0.068 & 0.069 \\
\midrule
Top-$B$ & 3.20 & 0.062 & 0.103 & 12.59 & 0.4ten& 0.513 \\
Rest-$B$ & 0.438 & 0.053 & 0.058 & 1.53 & 0.293 & 0.305 \\
All-$B$ & 0.464 & 0.053 & 0.058 & 1.63 & 0.294 & 0.307 \\
\bottomrule
\end{tabular}
\end{table}


The institution response to joint and separate rankings using $m=0001$ is quite different. There is no clique of institutions with an over-density of reference links, and the cardinal rankings of $E$ and $B$ are intermixed whether ranked together or separately. All but one of the top 20 institutions are designated $E$, although we note that institution labelling is at best an indication of a tendency to publish in one or the other field.

Figure~\ref{fig:eb_alignment} (left) exhibits a counterfactual, showing that when the clique is artificially removed the cardinal rankings of $E$ and $B$ sources are no longer sharply delineated between separate and joint ranking with $m=1000$, and some $B$ sources rise above $v=10$. The delineation is also diminished in Figure~\ref{fig:eb_alignment} (right), where recirculation through institutions spreads the reference concentration in the clique as discussed above. We discuss these processes below.
\begin{figure}[!htb]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_3ef_latex.pdf}
  \caption{Cardinal rankings for combined ($E+B$) versus separate ($E||B$) fields, using the source-only (m=1000) block (left) and bipartite block (right). In the left panel, the \textit{clique} (top ten $E$ sources) has been artificially removed. In the right panel, influence recirculates through institutions and is spread beyond the clique.}
  \label{fig:eb_alignment}
\end{figure}

\subsection{Time dependence}

The variation of unit influence over 25 years is illustrated in Figure~\ref{fig:fig6}, which plots $v$ for sources and institutions (when present in the window) across the five time windows $t_x=1,\ldots,5$ (2000--2024). The trend of the rank-adapted running means reveals that the influence hierarchy of units broadly persists over 25 years. As time passes, the volatility of individual ranks reduces and $v=1$ level encompasses more units; both effects are due to corpus growth as additional units rise above the production threshold ($\tau_{S,I}$).
\begin{figure}[!hbtp]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_6_latex.pdf}
  \caption{Influence $v$ against baseline ordinal rank across five
    time windows.
    Black line: baseline $t_x=5$ (2020--24).
    Coloured series: $t_x=1$ (2000--04) through $t_x=4$ (2015--19).
    $x$-axis locked to baseline ordinal rank. Units absent in earlier years
    are omitted only in those years.}
  \label{fig:fig6}
\end{figure}

Figure~\ref{fig:figstability} quantifies the achievement and persistence of
above-average influence - defined as $v>1$ - across the 25-year span.
Approximately 15\% of sources and 25\% of institutions have
above-average influence in each time window, and this fraction is stable over time.
Considering the 651~sources and 417~institutions present in both the
2000--04 and 2020--24 windows, $86\%$ of sources and $96\%$ of
institutions with above-average influence in 2000--04 retain that status
in 2020--24. The scatter plots exhibit the asymmetry: very few sources or institutions move from 
$v>1$ in 2000-04 to $v<1$ in 2020-24 (bottom right quadrant), and many move from $v<1$ to $v>1$
(upper left). Institutions that crossed the $v=1$ threshold between 2000--04 and 2020--24
are predominantly universities in Global North countries that expanded their economics and business research output over the period, consistent with the growth of internationally oriented business schools
and the associated increase in research-active faculty size. 

\begin{figure}[!tbp]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_stability_scatter_latex.pdf}
  \caption{Influence $v$ in 2000--04 against $v$ in 2020--24
    for units present in both windows (log--log) with baseline parameters.
    Left: sources ($n=651$).  Right: institutions ($n=417$).
    Diagonal: $v_{t_1}=v_{t_5}$. Hue encodes threshold-crossing status, e.g. red denotes the few units that decline across $v=1$ between 2000 and 2024.}
  \label{fig:figstability}
\end{figure}

\subsection{Parameter sensitivity}

In addition to the structural results presented above (units, subfields and time windows), the model includes several parameters whose choice, while arbitrary, alters the influence values.  Figure~\ref{fig:fig5} shows influence $v$ against baseline rank for five parameter variants, overlaid on the baseline. 

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_5_latex.pdf}
  \caption{Parameter sensitivity of influence $v$ against baseline
    ordinal rank.
    Left: sources.  Right: institutions.
    Black line: baseline ($\tau_I{=}10$, $\tau_S{=}5$, $\rho{=}0$, $\alpha{=}1$, $\varepsilon{=}0$).
    Red $\times$: $\tau{=}40$ ($N_S{=}663$, $N_I{=}1{,}125$).
    Blue $\times$: $\rho{=}1$.
    Green $\times$: census year 2024 only.
    Yellow $\times$: $\omega=1$.
    Purple $\times$: $\varepsilon=1$.
    $x$-axis locked to baseline rank; units absent from a variant are omitted.}
  \label{fig:fig5}
\end{figure}

Raising the work-per-year threshold to $\tau_I{=}\tau_S{=}40$ reduces the corpus size to around 40\% for sources and 57\% for institutions. Despite this reduction, the influence scores of the surviving units track the baseline closely, with Spearman coefficients $>~0.97$. However, the higher threshold removes low-ranked units differentially, raising influence for the most influential units and lowering it by about $0.1$ in $\log{v}$ for sources ($0.06$ for institutions) over the bulk of units. Consequently, the median absolute ordinal rank shift is large among the lower-influence population, 7.5\% for sources and 6.4\% for institutions that remain in the corpus. 

Removing the normalisation of reference list lengths ($\rho{=}1$)
leads to a small overall reduction of average source influence, $-0.07$ in $\log(v)$, while introducing substantial changes in individual influence values, (standard deviation - s.d. - in $\log(v)$ 0.2 for sources and 0.1 for institutions). These relatively large fractional variations occur across all units, leading to a median change in ordinal rank of 5\% for sources and institutions. 

The overlapping census and target windows for the baseline model `over-count' references that are close in publication date. When the census window is reduced to one year ($t_c=2024$), total reference counts reduce and many values of $v$ change  (s.d. 0.2 for sources, 0.1 for institutions). Because the reduction in citer-cited pairs occurs uniformly over $\mathbf{H}$ the shape of $v^{S,I}$ is similar to $v^B$.

Moving between institution weights based on author fractional counting and inform counting leads to very small changes in source $v$; for institutions the s.d. is 0.04 with no change in median value, and the median ordinal rank changes by less than 1.6\%.

When the baseline is modified by including the sentinel units ($\varepsilon=1$) the presence of the single large source and sink for all corpus units reduces the normalised size of within-corpus exchanges, flattening and shuffling the distribution of $v$ for sources and institutions. The median shift in $\log(v)$ is 0.3 for sources and 0.25 for institutions, and the median ordinal rank shift is around 10\%. We emphasise that the use of single sentinel units is only a coarse approximation to the flow of references to and from the corpus.

Commonly, bibliometric spectral rankings adopt the Katz-Hubbell (PageRank) algorithm, with an attenuation \citep{katzNewStatusIndex1953} $\alpha<1$ and a boundary condition \citep{hubbellInputoutputApproachClique1965} $\mathbf{\mu} \ne \mathbf{0}$ \citep{bollenJournalStatus2006, bergstromEigenfactorMetricsFigure2008}. With $\alpha=0.85$ and $\mu_p=1/(N_S+N_I)$, the baseline model is radically altered as shown in Figure~\ref{fig:fig5d}. Units that had low $v$ in the Perron-Frobenius model are held up by the floor $(1-\alpha)\mathbf{\mu}$ on the Katz-Hubbell eigenvector $\mathbf{\pi}$. Because influence $v_p \propto \pi_p/a_p$, units with only few works $a_p$ may further amplify this lift, leading to the occasional large rises seen in Figure~\ref{fig:fig5d}. Comparison between Figures~\ref{fig:fig5d} and \ref{fig:fig_3by} illustrates the straightforward interplay between the block models and the Katz-Hubbell algorithm: in the recirculating bipartite case, routing through units tends to spread influence compared with single-unit influence, although both are elevated by the boundary value $\mathbf{\mu}$. As noted above, we might choose $\mu = 1/(N_S+N_I)$ or $\mu= (1/2N_S, 1/2N_I)$ for sources and institutions. Unsurprisingly, with $N_S \approx N_I$ this choice makes little  difference, less than 0.02 in the log. While similar to the $\alpha=0.85$ model, the Eigenvector ranking \cite{bergstromEigenfactorMetricsFigure2008} excludes unit self-edges. We find that removing self edges systematically lowers $v$ when $v < 1$, by around 0.04 in the log for sources and institutions, while the variation around this shift is 0.08 (s.d.) for sources and 0.07 (s.d.) for institutions. For this data set, unit self-referencing contributes little to influence values or rank.  
\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_5d_latex.pdf}
  \caption{Comparing the Perron--Frobenius (black line), bipartite Katz--Hubbell (green points), and single-unit Katz-Hubbell (red points) spectral rankings, with $\alpha=0.85$ and uniform prior $\boldsymbol{\mu}$.     Left: sources.  Right: institutions. 
  The $x$-axis is locked to baseline  (Perron--Frobenius) rank.}
  \label{fig:fig5d}
\end{figure}

\subsection{Errors and uncertainties}

\cite{borgattiRobustnessCentralityMeasures2006} reported that, at that time, it was conventional wisdom that network analysis is intolerant to missing or mistaken data. Using random node and edge errors on random graph models, they offered a more optimistic view by showing that small errors allow the construction of confidence intervals around centrality scores. Favorable views of Eigenfactor/spectral methods for bibliometric ranking \citep[e.g.][]{franceschetTenGoodReasons2010} may incorporate this optimism. However, the propagation of citation network and bibliometric metadata errors into spectral ranking is unlike the additive random models studied by Borgatti et al. Moreover, as discussed by \cite{waltmanLeidenRanking20112012}, the interplay between skewed citation count distributions and different unit sizes can render indicators unstable to changes in the work set: accordingly, the Leiden ranking uses a bootstrap to establish stability intervals for its indicators. 

In the light of these concerns, we extend \cite{waltmanLeidenRanking20112012} to establish stability intervals for our influence scores. We construct 1000 replicates of the baseline corpus works by sampling with replacement \citep{efronBootstrapMethodsAnother1992}, and pass each replicate through the full pipeline, including the procedures for removing outlying and partially detached units.  
Figure~\ref{fig:boot_envelope_resample} displays the median as a blue dot and the stability interval as a red bar (extending between the 2.5-97.5\% centiles) over the replicates for each unit, against the unit's fractional rank in the baseline. Units absent from a replicate's connected component are excluded from the centile computation for that replicate. For the top $\approx$100 sources and institutions, the stability interval is narrow, and cardinal ranks are not greatly affected by changes in the set of works. For lower ranked units, the relative cardinal rank stability interval is $\approx\pm$15\%. The higher instability of the few units that are outliers in the median is due to their low number of retained works.
\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_boot_envelope_resample_latex.pdf}
  \caption{Stability intervals for fractional ranks in the baseline model. At each unit, a blue point denotes the median over 1,000 replicates, while the red vertical bar spans the 2.5--97.5 percentile interval of cardinal rank.}
  \label{fig:boot_envelope_resample}
\end{figure}

Work-driven instabilities are not the only source of uncertainty in influence scores. Like other bibliometric databases, OpenAlex includes a level of various kinds of metadata errors and uncertainties that might influence findings based on the data \citep{moedSynopsis2005}. We construct simple models of four potentially potent errors \citep{olenskyDataAccuracyBibliometric} and apply stochastic (Monte Carlo) simulation to propagate their effects into the spectral ranking. The errors are introduced at the work level and carried through the full non-linear influence pipeline.

Table~\ref{tab:oa_errors} summarises the uncertainty types and their adopted rates.
Publication year uncertainty generally arises from ambiguities between early-view, on-line and print years. The rate is estimated by examining the gaps in the concordance between recorded
publication year and the (Volume, Issue, Page) sequence for the $\approx$50\% of sources that have sequential metadata. Source misallocation, while rare, is observed in practice (e.g.\ some works from
\textit{Soshiki Kagaku} are attributed to \textit{Organization Science}).
Institution attribution errors can arise directly or via author attribution errors. Our estimate is based on finding several hundred works misattributed between Brown University and John Brown University, and other misidentifications of this kind. Referencing errors arise when reference lists are incorrectly transcribed, truncated, intermixed, or mis-indexed \citep[c.f.][]{olenskyEvaluationCitationMatching2016}; the adopted error is informed by our experience and the report of \cite{rodriguesDetectingIncoherentCitation2025}.
\begin{table}[htbp]
  \centering
  \small
  \caption{Models of errors used in the stochastic simulation of spectral ranking uncertainty.
           Errors are introduced at the work level and propagate through the pipeline using the baseline parameters. The rate is the proportion of the set in question (works or references) that is randomly affected in a replicate.}
  \label{tab:oa_errors}
  \begin{tabular}{lp{9cm}r}
    \toprule
    Error model       & Mechanism & Rate \\
    \midrule
    Publication year uncertainty
      & Random work from the time window [2018--2021] dropped or inserted into its source
        with the publication year shifted into the census window.
      & 2\% \\
    Source misattribution
      & Corpus work reallocated to a randomly chosen corpus source.
      & 0.03\% \\
    Institution misattribution
      & Corpus work reallocated to a randomly chosen corpus institution.
      & 1\% \\
    Reference misattribution
      & Corpus reference redirected to another work within the corpus
        (75\% within the same source; 25\% to another source)
      & 2\% \\
    \bottomrule
  \end{tabular}
\end{table}

The stochastic simulation first constructs 1000 replicates of the corpus works adjusted for each kind of work-level error: publication year and source/institution allocation. These are saved as differences from the baseline work set (Stage 1). It also constructs 1000 replicates of the work-level citer-cited table adjusted for reference list errors, saving differences from the baseline (Stage 2). Combining one Stage 1 replicate (work-level errors) with one Stage 2 replicate (reference errors) builds one replicate edge list at a time. Then our ranking algorithm applies the standard preprocessing and resolvent pipelines to generate 1000 replicates of the influence $\mathbf{v}$. To avoid the need to model author uncertainties, we adopt institutional fractionation to determine a work's institution weights.  

The top panels in Figure~\ref{fig:boot_ratio_facet} exhibit the uncertainty interval due to publication year uncertainties, modelled by randomly selecting 2\% of works from 2018-2021, dropping those from 2020-21 and inserting those from 2018-19 into the corpus. A dropped work is no longer a reference target, and its reference list is removed from the corpus. An inserted work becomes a potential reference target, but works in its reference list is likely precede the target window. With these changes, the relative rank of most replicates is hardly changed, but a few are substantially lowered (higher in Fig.~\ref{fig:boot_ratio_facet}) by the loss from a unit of highly referenced works. Errors in source attribution or institution attribution (central panels in Fig.~\ref{fig:boot_ratio_facet}) at the level of our model have a minor effect on influence or rank. We model reference errors by replacing 2\% of referenced works with a randomly selected work, from the same source (75\%) or another. Occasionally this places a highly referenced work into a unit with modest reference counts, raising its influence and rank  (i.e. lower in Fig.~\ref{fig:boot_ratio_facet})
\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_boot_ratio_facet_latex.pdf}
    \caption{Uncertainty intervals for fractional rank under four metadata error models. At each unit, a blue point denotes the median over 1000 replicates, while the red vertical bar spans the 2.5--97.5 centile interval. Left column: sources; right column: institutions.}
  \label{fig:boot_ratio_facet}
\end{figure}

Our model of OpenAlex errors is guided by others' experience \citep[e.g.][]{OpeningCWTSLeiden2024} but rests on plausible assumptions, not measured properties. The stochastic simulation functions as a sensitivity analysis. Errors at the level we have selected lead to insubstantial changes among the top $\approx$ 10\% of units. For lower ranks, the power-law distribution of citation frequencies for works, combined with units that just pass the volume threshold, leads to notable, albeit infrequent, changes in influence. Our experience in reviewing the influence values of units in our corpus is consistent with this: surprising placements can usually be explained by identifying an error or ambiguity in the metadata. The simulation findings are consistent with the empirical study presented by \cite{scheidstegerHowSimilarAre2025} who found low agreement between unit-level indicators derived from different large bibliometric databases including OpenAlex. 

\subsection{Enclaves}

In an important commentary on spectral ranking at the aggregate unit level, \cite{vaccarioWhenStandardNetwork2022} point out that the projection of work-work reference relationships creates `fictitious' information.  The mechanism is simple: consider the following references between three units $u$ and four works $w$: $w_1(u_1) \rightarrow w_{2a}(u_2)\, \& \, w_{2b}(u_2) \rightarrow w_3(u_3)$. The pattern contributes entries in the unit citation matrix, $u_1 \rightarrow u_2 \rightarrow u_3$, although there are no linked references. \cite{vaccarioWhenStandardNetwork2022} explore the implications by comparing source-source and work-work PageRank models with $\alpha=0.5$, concluding that `journal rankings may be wrong.'

An alternative viewpoint is that for \textit{some} purposes, projecting the work-work network prior to spectral ranking yields a useful ordering. For example, within the Social System of Citations Theory \citep{tahamtanSocialSystemsCitation2022}, research communications maintain the system. Sources and institutions are involved indirectly by maintaining infrastructure and professional expertise. While a one-step work-work reference reveals attention/influence transfer between the works, and perhaps indirect reliance on host units, a two-step chain reveals communication flow, but says little about reliance between remote host units. 
\begin{figure}[!htb]
  \centering
  \includegraphics[width=0.9\textwidth]{../plots/enclave_referer_v.pdf}
  \caption{Influence of highly cited works (HCW) against the median influence
    of their citing works. HCW in the lower-left quadrant (both $v<1$) 
    are published in low-influence units and cited predominantly by works from 
    low-influence units.}
  \label{fig:enclave_referer_v}
\end{figure}

We can explore some differences between work-work and unit-unit networks by examining highly cited works (HCW: citation counts in the top percentile for their publication year), since high citations are symptomatic of an extensive, coupled work-work reference network. Between 2014 and 2023, there are 8,675 HCWs in the corpus. Let us define the \textit{ranking of a work} $v_w$ as the median of its source and institutional cardinal rankings, and the ranking of a set of referencing works as the median of the separate work rankings. We are mindful of the cautionary advice of \cite{stevensTheoryScalesMeasurement1946} in relation to arithmetic operations on cardinal scales, but the eigenvector normalisation (Equation~\ref{eq:eig}) provides support for these combinations.  Figure~\ref{fig:enclave_referer_v} shows that HCWs with $v<1$ are overwhelmingly referenced by works with $v<1$, revealing a substantial population of highly cited works that is disconnected from units with high spectral ranking. This highlights the distinction between influence transfer at the work level and at the unit level.

To investigate further, we apply non-negative matrix factorization \citep{leeLearningPartsObjects1999} to the TF-IDF representation of the titles of all HCW with $v<1$, identifying $k=10$ large latent title-based topics that we consolidate by manual inspection into six named clusters. Five of the topic clusters form connected work-work communities while the `resource curse' cluster splits into two connected communities. The intriguing space between highly cited status and modest unit spectral ranking invites a deeper study of the works and researchers involved in these highly active topic communities, which we propose to call \textit{enclaves}.

\begin{table}[!htbp]
  \centering
  \caption{Topic enclaves are clusters of highly cited works (HCW) in units with $v<1$ that are also cited by works with median $v<1$.  Clusters are labelled using non-negative matrix factorization (NMF) on the HCW titles. Works is the count of the 1-hop neighborhood of the enclave HCWs (self plus other corpus works citing the HCWs); CC is the number of weakly connected components in the induced citation subgraph. $\langle v \rangle_{\text{HCW}}$ and $\langle v \rangle_{\text{citing}}$ are the median source influence of the HCW and of their citing works respectively.}
  \label{tab:enclaves}
  \small
  \begin{tabular}{lrrrrr}
    \toprule
    Enclave & $n_{\text{HCW}}$ & Works & CC & $\langle v \rangle_{\text{HCW}}$ & $\langle v \rangle_{\text{citing}}$ \\
    \midrule
    Sustainability & 666 & 39,654 & 1 & 0.749 & 0.693 \\
    Systematic literature reviews & 332 & 17,478 & 1 & 0.701 & 0.665 \\
    Digital transformation \& China & 304 & 13,140 & 1 & 0.694 & 0.735 \\
    COVID-19 \& financial markets & 265 & 14,807 & 1 & 0.712 & 0.628 \\
    Resource curse \& development & 160 & 8,744 & 2 & 0.728 & 0.621 \\
    Bitcoin \& policy uncertainty & 70 & 4,492 & 1 & 0.801 & 0.668 \\
    \bottomrule
  \end{tabular}
\end{table}