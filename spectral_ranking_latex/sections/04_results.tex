%!TeX root = ../main.tex
% ---------------------------------------------------------------
\section{Results}
\label{sec:results}
% ---------------------------------------------------------------


A glossary of the terms used in the model is exhibited in Table~\ref{tab:parameters}. There are three structural arrangements: (1) blocks, picked by the unit mask $m$ and encoded as $1000 \rightarrow S$ (sources only), $0001 \rightarrow I$ (institutions only), $0110 \rightarrow B$ (bipartite) and $1111 \rightarrow J$ (joint), (2) subfields, picked from $\mathfrak{F}$ (E, B, A, X), and (3) census and target time windows, overlapped ($t_c=t_t$) and picked from the sets \[\{T_t: t = 1 \ldots 5\} = \{[2000, 2004], [2005, 2009], [2010, 2014], [2015,2019], [2020,2024]\}.\] We also present a sensitivity analysis for the parameters $\rho,~\chi,~\omega,~\epsilon,~(\alpha,~\mu),~\rm{and}~ t_c$, together with a bootstrap study of uncertainties. Results focus on $\mathbf{v}_S$ and $\mathbf{v}_I$, the source and institution influence vectors, derived from the ranking probability by Equation \ref{eq:vstar}. An instance of the model is designated using a subscript for the unit and superscripts as required to designate structural arrangements and parameters. For example, $\mathbf{v}_I^{S,E,T_5}$ is the institution influence vector for the $S$ ($m=1000$) block, $F=E$ (Economics), and census and target time windows 2020-2024. Throughout this work, logarithms are base 10.

\input{../tables/table_parameters}

Figure~\ref{fig:fig_3by} compares influence values across the four unit masks. The $x$-direction is the cardinal rank of sources (left) and institutions (right) in the baseline (i.e. bipartite resolvent with $S \rightarrow I \rightarrow S,~\alpha=1,~\mu=0,~\rho=0,~\omega=0,~T_5 = [2020-2024]$ and $F=EBAX,~\rm{all~subfields}$). The solid black curve traces the corresponding influence value $v$ in the $y$-direction. Units near $v=1$ have average influence, corresponding to rank position $\approx~25\%$ for sources and $\approx~20\%$ for institutions. For most sources and institutions, $v$ lies between  1 and 0.1. Overlaid on the bipartite source curve are $v_S^S$ (red $\times$, $S \rightarrow S$) and $v_S^J$ (green $\times$, joint $S+I \rightarrow S+I$). A point on the $x$-axis is the same unit in every case. For institutions, we show $v_I^I$ ($I \rightarrow I$) and $v_I^J$ (joint). Figure~\ref{fig:fig_3bz} directly compares bipartite $v^B$ and single-unit $v^{S,I}$ influence, colouring points by subfield (see below).

\begin{figure}[!hbt]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_3by_latex.pdf}
  \caption{Logarithm of influence versus rank for 
    sources (left) and institutions (right).
    Black line: $v^B$ (bipartite, baseline);
    red circles: $v_S^S$ / $v_I^I$ (within-block);
    green diamonds: $v^J$ (joint $S+I$).
    $x$-axis is the cardinal rank for the baseline.}
  \label{fig:fig_3by}
\end{figure}

Notable features are the small proportion of high-$v$ units, the tightness of the institution dispersion compared with sources, and the difference between the source influence in the bipartite baseline $v_S^B$ and the source-only case $v_S^S$. These features arise from the structure of the citation matrices $H$ and $M$, in particular the way that references spread as they are given, and concentrate as they are received. To characterise this reference flow, consider the Shannon evenness $J$ \citep{stirlingGeneralFrameworkAnalysing2007} of an $n_{\text{row}}\times n_{\text{col}}$ row stochastic citation matrix $X~=~x_{ij}$. At row $i$ it is defined as 
\begin{equation}
  J_i = \frac{x_i}{\log n_{\text{col}}}, \qquad
  x_i = -\sum_j X_{ij}\log X_{ij},
  \label{eq:row_evenness}
\end{equation}
with $J_i\in[0,1]$. If a row distributes references uniformly $J=1$, while concentration of references to one column gives $J=0$. The mean over all rows,
\begin{equation}
  \bar{J} = \frac{1}{n_{\text{row}}}\sum_i J_i,
  \label{eq:J_bar}
\end{equation}
is relatively large when outward references are dispersed evenly across the whole corpus. The fan-out $\bar{K} = n_{\text{col}}^{\bar{J}}$ converts normalised row evenness to a count: the geometric mean number of distinct targets reached in one step over the Markov matrix.
A similar measure $J_{\text{col}}$ applies to the mean column sums $p_j = \sum_iX_{ij} / n_{\text{row}}$. Because X is row-stochastic, $p_j$ is a valid marginal distribution and
\begin{equation}
  J_{\text{col}} = \frac{-\sum_j p_j \log p_j}{\log n_{\text{col}}},
  \label{eq:J_col}
\end{equation}
measures its variability, with $J_{\text{col}}=1$ when units receive equal reference in-flow and $J_{\text{col}}\to 0$ when references flow to few units. Note that $p_j$ is a version of the journal impact factor (JIF). 


\input{../tables/table_kernel_structure}


Table~\ref{tab:kernel_structure} quantifies how, in one step, institutions spread outgoing references more broadly than sources ($\bar{J}_{II}=0.74$ vs $\bar{J}_{SS}=0.50$; fan-out $7.3\times$ larger), while receiving incoming references only slightly more broadly than sources ($J_{col, II}=0.95$ vs $J_{col, SS}=0.92$). In two-step kernels, $M_S$ spreads references more evenly than $C_{SS}$ ($\bar{J}:$ 0.84 c.f. 0.50) while also narrowing the spread of incoming references ($J_{\text{col}}$: 0.87 c.f. 0.92). The bipartite round-trip spreads attention/influence broadly while rewarding the most-cited sources, raising $v$ for high-ranked sources as shown in Figs~\ref{fig:fig_3by} and \ref{fig:fig_3bz}: normalisation of influence then reduces $v$ for lower ranked sources. For institutions, $M_I$ is more balanced ($\bar{J}=0.90$, $J_{\text{col}}=0.93$): bipartite paths make little difference compared with institution-only paths, although $v_I^I$ is slightly less than $v_I^B$ for the highest ranks. The joint resolvent covering the full citation matrix yields v between the bipartite and single-unit value.

\begin{figure}[!htb]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_3bz_latex.pdf}
  \caption{Bipartite vs within-layer influence for the full corpus.
    Sources (left): $\log v_S^S$ vs $\log v_S^B$.
    Institutions (right): $\log v_I^I$ vs $\log v_I^B$.
    Points coloured by subfield (E, B, A, X).}
  \label{fig:fig_3bz}
\end{figure}

With this understanding of the role of unit masking, we now explore the effects of subfields. Figure~\ref{fig:fig_3by} shows that subfields stand out in unit influence values, even when subfields are not used in the ranking. The prominent effect is the differential response of $E$ and $B$ sources to the bipartite ranking compared with the single unit ranking. Specifically, influence boost due to greater unit concentration as described above is a feature of $E$-sources rather than $B$-sources. The separation exemplifies the observation of \cite{pinskiCitationInfluenceJournal1976} and \cite{waltmanSourceNormalizedIndicators2013} that spectral ranking is sensitive to the presence of subfields: not only are there different responses, but these responses may themselves alter the rankings and influence scores.  

Figure~\ref{fig:fig_2c} illustrates the process by exhibiting influence distribution when unit sets are truncated to lie within the $E$ or $B$ subfield. Rather than the subfield separation seen in Figure~\ref{fig:fig_3by}, subfields are mingled over similar distributions. Both $B$ and $E$ sources attain similar values of high influence, with influence for $B$-sources rising by around 10-20\% and $E$-sources falling by a comparable amount compared with ranking over the combined subfields. The different behavior between combined and separated subfields is apparent in the second eigenvector of the combined corpus (not shown), where the two subfields are cleanly separated by sign. Subfield separation occurs despite the relatively low modularity of the full corpus, 0.2 for the source-only network, 0.07 for institutions, and 0.002/0.0001 for the two forms ($S \rightarrow I$ and $I \rightarrow S$) of the bipartite network.

\begin{figure}[!htb]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_2c_latex.pdf}
  \caption{Bipartite vs within-layer influence, evaluated separately for each subfield.
    Sources (left): $\log v_S^S$ vs $\log v_S^B$.
    Institutions (right): $\log v_I^I$ vs $\log v_I^B$.
    Points coloured by subfield (E, B, A, X).}
  \label{fig:fig_2c}
\end{figure}

\paragraph{Temporal structure}
Figure~\ref{fig:fig6} plots $v$ for all sources and institutions across
the five time windows $t_x=1,\ldots,5$ (2000--2024), with the $x$-axis
locked to the baseline rank. The running mean trend shows that the influence hierarchy is persistent over 25 years.  
The Spearman correlation between $v$ in 2000--04 and $v$ in 2020--24 is moderate, $\rho_S=0.663$ for sources and $\rho_S=0.660$ for institutions on the
common sets. The consecutive-window values are $\rho_S=0.905$ (sources) and
$\rho_S=0.864$ (institutions), implying that the scatter in Figure~\ref{fig:fig6} arises from long-run drift rather than short-run
volatility. Institutions are no less stable in rank than sources despite having
more volatile absolute $v$ values across windows, reflecting the
growing size of the institution corpus as additional universities
cross the $\tau_I$ threshold over the 25-year period.
The greater smoothness of the institution panel in Figure~\ref{fig:fig_3by}
and the comparable long-run rank stability in Figure~\ref{fig:fig6}
point to the same mechanism: the $SI$--$IS$ coupling acts as a
stabiliser, averaging out the local fluctuations in citation practice
that would otherwise produce volatile source rankings.

\begin{figure}[!hbtp]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_6_latex.pdf}
  \caption{Influence $v$ against baseline rank across five
    time windows.
    Black line: baseline $t_x=5$ (2020--24).
    Coloured series: $t_x=1$ (2000--04) through $t_x=4$ (2015--19).
    $x$-axis locked to baseline rank; units absent from the window
    are omitted from that series.}
  \label{fig:fig6}
\end{figure}

Figure~\ref{fig:figstability} quantifies the achievement and persistence of
above-average influence - defined as $v>1$ - across the 25-year span.
Approximately 25\% of sources and 20\% of institutions have
above-average influence in each window, and this fraction is stable over time.
Considering the 835~sources and 606~institutions present in both the
2000--04 and 2020--24 windows, $83\%$ of sources and $94\%$ of
institutions with above-average influence in 2000--04 retain that status
in 2020--24. The scatter plots exhibit the asymmetry: the upper-right
quadrant (influence maintained over 25 years) dominates, while the lower-right
quadrant (influence lost) is nearly empty.
Classifying each unit's trajectory across $v=1$ as a binary sequence (H if $v>1$, L otherwise) across the five windows shows that monotone paths dominate
among institutions (101~monotone rises vs 29~oscillating), while for
sources the counts are closer (74~monotone vs 80~oscillating).
Once an institution crosses $v=1$ it frequently stays above it, while 
sources exhibit an upward trend while moving above and below $v=1$.
The institutions that crossed the threshold between 2000--04 and 2020--24
are predominantly universities in Global North countries that have expanded their economics and business research output over the period,
consistent with the growth of internationally oriented business schools
and the associated increase in research-active faculty size. 

\begin{figure}[!tbp]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_stability_scatter_latex.pdf}
  \caption{Influence $v$ in 2000--04 against $v$ in 2020--24
    for units present in both windows (log--log).
    Left: sources ($n=835$).  Right: institutions ($n=606$).
    Hue encodes threshold-crossing status.
    Dotted diagonal: $v_{t_1}=v_{t_5}$.
    Dashed lines: $v=1$ on each axis.
    Parameters: $F=A$, $\tau_I=\tau_S=20$, $\rho=0$,
    $\mathbf{m}=0110$, $\alpha=1$.}
  \label{fig:figstability}
\end{figure}

In addition to the structural results presented above (units, subfields and time windows), the model includes several parameters whose choice, while arbitrary, alters the influence values.  Figure~\ref{fig:fig5} shows influence $v$ against baseline rank for four parameter variants, overlaid on the baseline. 

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_5_latex.pdf}
  \caption{Parameter sensitivity of influence $v$ against baseline
    rank.
    Left: sources.  Right: institutions.
    Black line: baseline ($\tau_I{=}10$, $\tau_S{=}5$, $\rho{=}0$, $\alpha{=}1$, $\varepsilon{=}0$).
    Purple $\times$: $\tau{=}40$ ($N_S{=}663$, $N_I{=}1{,}125$).
    Orange $\times$: $\rho{=}1$.
    $x$-axis locked to baseline rank; units absent from a variant are omitted.}
  \label{fig:fig5}
\end{figure}

Raising the work-per-year threshold to $\tau_I{=}\tau_S{=}40$ reduces the corpus size by around 40\% for each unit. Despite this reduction, the influence scores of the surviving units track the baseline closely, with Spearman coefficients $>~0.98$. However, the higher threshold removes low-ranked units differentially, raising influence for high-$v$ units and lowering it by about $0.04$ for sources and $0.07$ in $\log{v}$ for the bulk of units. Consequently, the median absolute rank shift is large among the lower-influence population, $167$ for sources and $189$ for institutions that remain in the corpus. 

Removing the normalisation of reference list lengths ($\rho{=}1$)
leads to a small overall reduction of average source influence, 0.05 in $\log{r=\Delta(v)/v}$, while introducing substantial changes in individual influence values, s.d. $\log{r} = 0.2$ for sources and s.d. $\log{r} = 0.1$ for institutions. These relatively large fractional variations occur across all units, leading to a median change in rank of 5\% for sources and institutions. 

The overlapping census and target windows for the baseline model `over-counts' references that are close by publication date. When the census window is reduced to one year ($t_c=2024$), total reference counts reduce and many values of $v$ change  (s.d. $log(r) = 0.2 (\rm{sources}), 0.1 (\rm{institutions})$). Because the reduction in citer-cited pairs occurs uniformly over $\mathbf{H}$ the shape of $v^{S,I}$ is similar to $v^B$.

Moving between institution weights based on author fractional counting and inform counting leads to very small changes in source $v$; for institutions $s.d. \log{r} = 0.04$ with no change in median value, and the median rank changes by 27 places.

The introduction of the Katz-Hubble damping parameter $\alpha=0.85$ and prior $\mu=1/N_p, p~\in~\mathfrak{P}$ leads to the marked changes exhibited in Figure~\ref{fig:fig5d}. The primary driver is the support for $v$ provided by the prior $\mu$: with any $\alpha < 1$ the stationary influence probability $\pi$ receives a contribution proportional to $(1{-}\alpha)\mu_p$ at every unit, establishing a floor to $\pi_p$. When converted to 
influence $v_p \propto  \pi_p / a_p$, variations in work count $a_i$
lead to variations in $v$, including large rises for a few units having very  small work: the $\approx 20$  units that stand well above the bulk of source influences values have only 2--4 works in the baseline corpus. The floor mechanism is present in both bipartite (baseline) and single-unit influence values when the Katz-Hubbell (or PageRank) formulation is adopted.
The choice of prior (uniform $1/N$ vs.\ unit-scaled $1/N_p$) shifts
the floor level slightly but does not change the qualitative pattern and is not shown here. The Eigenvector ranking \cite{bergstromEigenfactorMetricsFigure2008}, while similar to this model, excludes unit self-references. Testing this, we find only a slight reduction of the median source rank (82 to 79), while the median influence rank reduces from 136 to 118 due to the greater dispersion of references over institutions. 

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_5d_latex.pdf}
  \caption{Parameter sensitivity of influence $v$ to Katz-Hubbell modifications.
    Left: sources.  Right: institutions.
    $x$-axis locked to baseline rank; units absent from a variant are omitted.}
  \label{fig:fig5d}
\end{figure}

We now ask how uncertainties and errors in OpenAlex appear in spectral rankings. The sets of units used in our models are samples from a larger population of sources and institutions, so sampling error arises. In addition, like all bibliometric databases, OpenAlex includes a variety of mistakes and uncertainties, some relatively unimportant for this study (e.g. errors in language identification, ambiguities in names, misreported open access status). Others lead to uncertainties in the values of influence, and we aim to estimate their size. To do this we use non-parametric bootstrapping {efronBootstrapMethodsAnother1992} to model sampling error, and stochastic simulation of four kinds of OpenAlex metadata statistical errors. Errors are introduced at the work level, and carried through all steps of the non-linear algorithm that yields baseline influence.  

Sampling error is modelled by drawing samples with replacement from the full set of baseline works. Publication year uncertainties are widespread in bibliometric databases, affecting corpus membership on the citer and cited side. Not necessarily `wrong', they arise through different treatments of preprint date, early-access publication dates, print/electronic archival dates, and metadata errors. To explore uncertainty in OpenAlex publication years we examined gaps in the concordance between publication year and the (Volume, Issue, Page) sequence for sources having such metadata (many electronic sources do not include sequential descriptors). From this study we model publication year uncertainty for the [2020, 2024] baseline by randomly choosing a set of corpus works from the 4-year window [2016-2019] preceding the earliest target date, and inserting them into the same source with their publication year changed to lie in the census window. The number of works replaced per sample is 2\% of the number of works in the census window, $A$. While apparently rare, we found from the sequence study that works are sometimes allocated to erroneous sources (e.g. works from Soshiki Kagaku are attributed to Organization Science): we model this by randomly altering the source of 0.03\% of census works. Errors are also made in the assignment of works to institutions. An unpublished study by Andreas Pacher at TU Wien suggests that even though OpenAlex sets the standard for work-institution accuracy, the error level is nevertheless around 0.5\%. Our investigations have found obvious ambiguities including several hundred works miss-attributed between Brown University/John Brown University, Universidad del Noreste/Northeastern University, and Anderson University SC/Anderson School UCLA. We have removed these smaller institutions entirely for this study, without allocating their works to the larger. We model institution attribution errors as a random reallocation of 1.0\% of works' institutions by another within the corpus: the model could be improved by including institutional name textual similarity. Errors occur in reference lists when they are truncated or intermixed with other lists, or when an intended reference is miss-allocated by indexers to another work within the corpus. We model reference list errors by pointing 2\% of references to another work, 75\% within the same source and the remainder to another source.

The stochastic simulation first constructs 1000 replicates of the corpus works adjusted for each kind of work variation: sampling, publication year, and source/institution allocation, saving differences from the baseline work set (Stage 1). It also constructs 1000 replicates of the work-level citer-cited table adjusted for reference list errors, saving differences from the baseline (Stage 2). Sampling works randomly from the four Stage 1 sets and reference connections from Stage 2, the algorithm applies the standard preprocessing and resolvent pipelines to generate 1000 replicates of the influence $\mathbf{v}$. To avoid the need to model author uncertainties, we adopt institutional fractionation to determine a work's institution weights.  

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{../plots/fig_7a_oa_errors_latex.pdf}
  \caption{Stochastic simulation of 1000 replicates of OpenAlex uncertainties and errors on the 
  spectral ranking of the baseline model. The black line is the baseline and the red line the replicate median showing only minor deviation. The replicates of each unit are plotted as grey points, with a blue dot on the upper and lower extremes, showing that extreme variations occasionally arise.}
  \label{fig:fig_7}
\end{figure}

Figure~\ref{fig:fig_7} summarises the 1000 replicates, highlighting the large deviations in $v$ and thus rank that may arise. Among sources with baseline $v \geqslant 1$, around 50\% of replicates have one source with a twofold or more increase or decrease in its $v$, with increases outnumbering decreases 5:1. This is often due to alteration of the publication year of well-referenced work that is inserted into a source with relatively few works, sharply raising its prominence in the citation matrix. Less common but also responsible for large increases is the reallocation of a reference from the source's works to a different work. In some replicates a source has a large decrement, generally due to the result of a model sampling error deleting a well-referenced work from a source with relatively few works. Around 15\% of replicates have an institution with a twofold or more change in $v$, with around 3:1 downward. Most of these changes are due to deleting corpus works that referenced the institution's works.

Our models of OpenAlex errors are plausible assumptions, not measured properties. The stochastic simulation functions as a sensitivity analysis, showing the implications for spectral rankings if the errors occur in this way and at this rate. The power-law distribution of citation frequencies for works, sources and units, combined with the presence of many units that just pass the productivity threshold, leads inevitably to rare large changes in the place of a unit in the ranking. Our experience in reviewing the influence values of units is consistent with this: surprising placements are usually explained by identifying an error or ambiguity in the metadata. This finding from our simulation is consistent with the empirical study presented by \citep{scheidstegerHowSimilarAre2025} which found low agreement between unit-level indicators derived from 
different large bibliometric databases including OpenAlex. 