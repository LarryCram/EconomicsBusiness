%!TeX root = ../main.tex
% ---------------------------------------------------------------
\section{Results}
\label{sec:results}
% ---------------------------------------------------------------

The model supports three structural arrangements that we present in order: (1) blocks, picked by the unit mask $m$ and encoded as $1000 \rightarrow S$ (sources only), $0001 \rightarrow I$ (institutions only), $0110 \rightarrow B$ (bipartite) and $1111 \rightarrow J$ (joint), (2) subfields, picked from $\mathfrak{F}$ (E, B, A, X), and (3) census and target time windows, overlapped ($t_c=t_t$) and picked from the sets \[\{T_t: t = 1 \ldots 5\} = \{[2000, 2004], [2005, 2009], [2010, 2014], [2015,2019], [2020,2024]\}.\]We also present a sensitivity analysis for the parameters $\rho,~\chi,~\omega,~(\alpha,~\mu),~\rm{and}~ t_c$, together with a bootstrap study of uncertainties. Results focus on $v_S$ and $v_I$, the source and institution influence vectors, derived from the ranking probability by Equation \ref{eq:vstar}. An instance of the model is designated using a subscript for the unit and superscripts to designate block arrangements and parameters. For example, $v_I^{S,E,T_5}$ is the institution influence vector for the S ($m=1000$) block, $F=E$ (Economics), and census and target time windows 2020-2024. 

Figure~\ref{fig:fig_3by} presents key results, comparing influence values across the four unit masks. The $x$-direction is the falling cardinal rank of sources (left) and institutions (right) in the baseline (i.e. bipartite with $S \rightarrow I \rightarrow S,~\alpha=1,~\mu=0,~\rho=1,~\omega=1.~~T_5 = [2020-2024]$ and $F=EBAX,~\rm{all~subfields}$). The solid black curve traces the corresponding influence $v$ in the $y$-direction. Units near $v=1$ have average influence, corresponding to rank position $\approx~25\%$ for sources and $\approx~20\%$ for institutions. Overlaid on the bipartite source curve are $v_S^S$ (red $\times$, $S \rightarrow S$) and $v_S^J$ (green $\times$, joint $S+I \rightarrow S+I$). A point on the $x$-axis is the same unit in every case. For institutions, we show $v_I^I$ ($I \rightarrow I$) and $v_I^J$ (joint).

\begin{figure}[!hbt]
  \centering
  \includegraphics[width=0.65\textwidth]{../plots/fig_3by_latex.pdf}
  \caption{Logarithm of influence versus rank for 
    sources (left) and institutions (right).
    Black line: $v^B$ (bipartite, baseline);
    red circles: $v_S^S$ / $v_I^I$ (within-block);
    green diamonds: $v^J$ (joint $S+I$).
    $x$-axis is the cardinal rank for the baseline.}
  \label{fig:fig_3by}
\end{figure}

Notable are the tightness of the institution dispersion compared with sources, and the difference between the source influence in the bipartite baseline $v_S^B$ and the source-only case $v_S^S$. These features arise from the structure of the citation matrices $H$ and $M$, in particular the way that references spread as they are given, and concentrate as they are received. To measure this, consider the Shannon evenness $J_i$ \citep{stirlingGeneralFrameworkAnalysing2007} of an $n_{\text{row}}\times n_{\text{col}}$ row stochastic citation matrix $X~=~x_{ij}$. At row $i$ it is  
\begin{equation}
  J_i = \frac{x_i}{\log n_{\text{col}}}, \qquad
  x_i = -\sum_j X_{ij}\log X_{ij},
  \label{eq:row_evenness}
\end{equation}
with $J_i\in[0,1]$. If a row has uniform probabilities $J=1$, while a concentration at one referenced source gives $J=0$. The mean over all rows,
\begin{equation}
  \bar{J} = \frac{1}{n_{\text{row}}}\sum_i J_i,
  \label{eq:J_bar}
\end{equation}
is larger when outward references are dispersed across the whole corpus.
A similar measure $J_{\text{col}}$ applies to the mean column sums $p_j = \sum_iX_{ij} / n_{\text{row}}$, namely
\begin{equation}
  J_{\text{col}} = \frac{-\sum_j p_j \log p_j}{\log n_{\text{col}}},
  \label{eq:J_col}
\end{equation}
with $J_{\text{col}}=1$ when units receive equal reference in-flow and $J_{\text{col}}\to 0$
as in-flow concentrates on a single unit. The fan-out $\bar{K} = n_{\text{col}}^{\bar{J}}$ converts normalised row evenness to a count: the geometric mean number of distinct targets reached in one step over the Markov chain.


\input{../tables/table_kernel_structure}


Table~\ref{tab:kernel_structure} quantifies how institutions spread outgoing references far more broadly than sources ($\bar{J}_{II}=0.74$ vs $\bar{J}_{SS}=0.50$; fan-out $7.3\times$ larger),
yet concentrate incoming source references more sharply than any other kernel
($J_{\text{col}}(C_{IS})=0.87$).

Both effects carry through to the two-step kernels.
For sources, $M_S$ combines high row evenness ($\bar{J}=0.84$) with the same
low column evenness ($J_{\text{col}}=0.87$): the bipartite round-trip spreads
probability broadly but still rewards the most-cited journals,
producing the fan-shaped scatter in Figure~\ref{fig:fig_3bz}.
For institutions, $M_I$ is more balanced ($\bar{J}=0.90$, $J_{\text{col}}=0.93$):
the bipartite path equalises most institutions while concentrating
the top slightly more than the direct path alone, so $v_I^B$
exceeds $v_I^I$ only at the highest ranks.

\begin{figure}[!htb]
  \centering
  \includegraphics[width=\textwidth]{../plots/fig_3bz_latex.pdf}
  \caption{Bipartite vs within-layer influence.
    Sources (left): $\log v_S^S$ vs $\log v_S^B$.
    Institutions (right): $\log v_I^I$ vs $\log v_I^B$.
    Points coloured by subfield (E, B, A, X).}
  \label{fig:fig_3bz}
\end{figure}

% To account for the wide dispersion of $v_S^S$ against $v_S^B$, note that $v_S^S$ is a realisation of the direct journal-journal citation network studied by \cite{pinski, westEigenfactorMetricsNetwork2010, bollenJournalStatus2006} and \cite{franceschet} and used 
% to form the Clarivate Article Influence Score (AIS). A least-squares fit of the logarithmic 2023 AIS for our journal set against $v_S^S$
% follows $\mathrm{AIS}\propto (v_S^{S}){^{0.91}}$ ($R^2=0.82$), confirming alignment despite differences in corpus, time coverage,
% and inclusion of unit self-references. For $v_S^S$ the influence of a journal reflects the influence of the journals that reference it. On the other hand, in the bipartite case $v_S^B$, the influence of a journal reflects the influence of the institutions that reference it. 

% Figure~\ref{fig:fig3a} and Table~\ref{tab:unit_effects} illustrates this coupling in action. 
% The Figure shades each source with a hue representing the mean weighted influence of the institutions that are connected to it: sources with relatively high $v_S^S$ have relatively high $v_I^B$, compared with sources having low $v_S^S$ and $v_I^B$. The balancing for some units can be delicate, as shown in Table~\ref{tab:unit_effects}. It exhibits eight samples of this behavior, selected as extremes (upper, lower, left, right)  along the `average unit' 
% region around $v=1$. Among the sources, note how the source  has $v_{\rm bip} > v_{\rm SS}$ due to the high influence of the institutions that reference it, while  (ISSN 2225-1146, Q2) has $v_{\rm bip} < v_{\rm SS}$ despite the high influence of its referencing institutions since the sources that reference it are of lower influence. Games Econ\. Behav\. has very high $v_{\rm SS}$ which is lowered by the high, but lower $\bar{v}_{I}^{\rm bip}$, while Eon\. Geog\. has high $v_{\rm SS}$ raised further by high $\bar{v}_{I}^{\rm bip}$. The spread between institution $v_{\rm II}$ and $v_bip$ is lower than for sources, and for all four cases shown, the value of $\bar{v}_I^{bip}$ determines whether 
% $v_bip > v_{II}$

\input{../tables/table_unit_effects}

Figure~\ref{fig:fig2} illustrates how subfields affect ranking. For sources, 
the three field subsets are $v_S^E$ (economics-dominant
sources, $N_S=384$), $v_S^B$ (business-dominant, $N_S=601$), and $s_S^A$
(ambiguous sources present in both fields, $N_S=133$).
The $x$-axis is locked to the all-sources baseline rank and each unit
appears at its baseline position. Sources absent from a subset are 
omitted from that series. The LOWESS trend lines (bandwidth $f=5/n$, fitted on $\log v$) 
summarise each overlay.

Restricting to $F=E, B, \rm{or} A$ {lowers} the trend line of $v_S$ compared with 
the baseline across most of the full rank range, less so for $F=B$ and more so for $F=A$.
While $v_S^B(E,B,A)$ receives weighted influence from most institutions, $v_S^S(E,B,A)$ is increased by a few highly ranked sources (see inset) so that low-ranked sources have reduced influence due to normalisation.   

The institution panel tells a more nuanced story.
For the top 30--40 institutions by baseline rank the $F{=}B$ trend lies
\emph{above} baseline while $F{=}E$ lies below it, but below rank~40 both
overlays converge back toward the baseline.
The high-$v$ institutions are E-specialists---Federal Reserve Banks, NBER,
CEPR---that gain influence when the $F{=}E$ corpus removes the
business sources that dilute the eigenvector signal.
Conversely, they lose ground in $F{=}B$.
The low-$v$ tail of $F{=}E$ institutions (roughly 70 units with $v < 0.1$
at baseline rank $<2000$) are nearly disconnected from economics sources:
their economics citation share is below 8\%, and their $\mathtt{field\_eb}$
labels are predominantly $X$ or $B$.

To test whether the field shift is mediated by the institution layer or
is intrinsic to source--source citation structure, we compare v-ratios
$v_F / v_{\rm base}$ under $\mathbf{m}{=}0110$ (bipartite, institutions in
the loop) and $\mathbf{m}{=}1000$ (source--source only).
Table~\ref{tab:field_modes} reports the results.
When institutions are excluded, restricting to $F{=}E$ reduces source $v$
by 38\% at the median (ratio 0.62, 0.8\% of sources above baseline);
with institutions included the penalty shrinks to 9\% (ratio 0.91,
27\% above baseline).
The mirror holds for $F{=}B$: sources gain 51\% under $m{=}1000$ but only
3\% under $m{=}0110$.
A paired Wilcoxon test confirms that the bipartite ratio exceeds the
SS-only ratio for both fields ($p < 10^{-62}$).

This pattern is a direct consequence of the smoothing identified
in Section~\ref{sec:unit_effects}: because the stationary-weighted
effective branching $\bar{m}_\pi$ is 3.5 times larger for the institution
stochastic matrix $H_{II}$ than for sources $H_{SS}$, each step through
an institution fans out influence across a wide mix of sources from both
fields.
The institution layer therefore acts as a cross-field mixer, substantially
attenuating the eigenvector response to corpus field restriction.
This is consistent with \citet{waltman_vaneck}, who showed that
influence-score rankings are sensitive to corpus field composition, but the
institution channel diffuses that sensitivity, so the bipartite ranking
is more robust to field-scope choice than any pure source--source method.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{../plots/fig_2_latex.pdf}
  \caption{Influence $v$ against baseline rank by field scope
    ($\mathbf{m}{=}0110$, $\tau_I{=}\tau_S{=}20$, $\rho{=}0$, $\alpha{=}1$,
    $t_x{=}5$, 2020--24).
    Left: sources ($N_S^{\rm base}{=}1{,}626$).
    Right: institutions ($N_I^{\rm base}{=}2{,}629$).
    Black line: baseline (all sources).
    Red $+$: $F{=}E$ ($N_S{=}384$).
    Blue $+$: $F{=}B$ ($N_S{=}601$).
    Orange $+$: $F{=}A$ ($N_S{=}133$).
    Thick coloured curves: LOWESS trend on $\log v$.
    Dashed line: $v{=}1$.
    $x$-axis locked to baseline rank; absent units omitted.}
  \label{fig:fig2}
\end{figure}

\paragraph{Temporal structure}
Figure~\ref{fig:fig6} plots $v$ for all sources and institutions across
the five time windows $t_x=1,\ldots,5$ (2000--2024), with the $x$-axis
locked to the baseline rank.
The influence hierarchy is evidently persistent over 25 years, 
The Spearman correlation between $v$ in 2000--04 and $v$ in 2020--24 is
$\rho_S=0.663$ for sources and $\rho_S=0.660$ for institutions on the
common sets;
for consecutive windows it rises to $\rho_S=0.905$ (sources) and
$\rho_S=0.864$ (institutions), so the bulk of the apparent instability
in Figure~\ref{fig:fig6} is long-run drift rather than short-run
volatility.
Institutions are no less stable in rank than sources despite having
more volatile absolute $v$ values across windows, which reflects the
growing size of the institution corpus as additional universities
cross the $\tau_I$ threshold over the 25-year period.
The greater smoothness of the institution panel in Figure~\ref{fig:fig_3by}
and the comparable long-run rank stability in Figure~\ref{fig:fig6}
point to the same mechanism: the $SI$--$IS$ coupling acts as a
stabiliser, averaging out the local fluctuations in citation practice
that would otherwise produce volatile source rankings.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{../plots/fig_6_latex.pdf}
  \caption{Influence $v$ against baseline rank across five
    time windows.
    Black line: baseline $t_x=5$ (2020--24).
    Coloured series: $t_x=1$ (2000--04) through $t_x=4$ (2015--19).
    $x$-axis locked to baseline rank; units absent from the window
    are omitted from that series.}
  \label{fig:fig6}
\end{figure}

Figure~\ref{fig:figstability} quantifies the persistence of
above-average influence---defined as $v>1$, meaning the unit
absorbs more influence than the corpus average per work---across the full
25-year span.
Approximately one quarter of sources and one quarter of institutions have
above-average influence in each window, and this fraction is stable across time,
confirming that $v=1$ is a structurally consistent threshold rather than
a feature of any particular window.
Restricting to the 835~sources and 606~institutions present in both the
2000--04 and 2020--24 windows, $83\%$ of sources and $94\%$ of
institutions with above-average influence in 2000--04 retain that status
in 2020--24.
The scatter plots confirm this asymmetry visually: the upper-right
quadrant (surplus in both windows) dominates, while the lower-right
quadrant (surplus lost) is nearly empty.
Classifying each unit's trajectory as a binary sequence (H if $v>1$,
L otherwise) across the five windows shows that monotone paths dominate
among institutions (101~monotone rises vs 29~oscillating), while for
sources the counts are closer (74~monotone vs 80~oscillating).
Once an institution crosses $v=1$ it almost invariably stays above it;
sources are somewhat noisier, but the direction is sustained.
The institutions that crossed the threshold between 2000--04 and 2020--24
are predominantly universities in Western countries that expanded their
economics and business research output substantially over the period,
consistent with the growth of internationally oriented business schools
and the associated increase in faculty size.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{../plots/fig_stability_scatter_latex.pdf}
  \caption{Influence $v$ in 2000--04 against $v$ in 2020--24
    for units present in both windows (log--log).
    Left: sources ($n=835$).  Right: institutions ($n=606$).
    Colour indicates threshold-crossing status:
    blue = above-average influence ($v>1$) in both windows;
    light blue = below threshold in both;
    green = crossed $v=1$ upward;
    red = crossed $v=1$ downward.
    Dotted diagonal: $v_{t_1}=v_{t_5}$.
    Dashed lines: $v=1$ on each axis.
    Parameters: $F=A$, $\tau_I=\tau_S=20$, $\rho=0$,
    $\mathbf{m}=0110$, $\alpha=1$.}
  \label{fig:figstability}
\end{figure}

\paragraph{Parameters}

Figure~\ref{fig:fig5} shows influence $v$ against baseline rank for
five parameter variants, overlaid on the baseline ($\tau_I{=}\tau_S{=}20$,
$\rho{=}0$, $\alpha{=}1$, $\mathbf{m}{=}0110$, all sources, 2020--24).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{../plots/fig_5_latex.pdf}
  \caption{Parameter sensitivity of influence $v$ against baseline
    rank.
    Left: sources.  Right: institutions.
    Black line: baseline ($\tau_I{=}\tau_S{=}20$, $\rho{=}0$, $\alpha{=}1$).
    Purple $\times$: $\tau{=}40$ ($N_S{=}945$, $N_I{=}1{,}533$).
    Orange $\times$: $\rho{=}1$.
    Blue $\times$: census window 2024 only ($t^c{=}1$yr).
    Red $\times$: $\alpha{=}0.85$, $\mu{=}1/N$ (uniform Katz prior).
    Green $\times$: $\alpha{=}0.85$, $\mu{=}1/N_p$ (unit-scaled prior).
    $x$-axis locked to baseline rank; units absent from a variant are omitted.}
  \label{fig:fig5}
\end{figure}

\textit{Threshold $\tau$.}
Raising both thresholds to $\tau_I{=}\tau_S{=}40$ reduces the corpus from
$N_S{=}1{,}626$ sources and $N_I{=}2{,}629$ institutions to $N_S{=}945$
and $N_I{=}1{,}533$, a reduction of roughly 40\% in each set.
Despite this large change in corpus composition, the influence scores of
the surviving units track the baseline almost exactly:
the lowess trend for $\tau{=}40$ overlaps the baseline across the full rank
range.
The ranking is therefore robust to the choice of activity threshold within
the range studied.

\textit{Reference weighting $\rho$.}
Setting $\rho{=}1$ (equal attention per reference, as in a standard random
walk) produces no visible change relative to the baseline ($\rho{=}0$,
equal attention per work).
The two weighting schemes yield essentially the same stationary distribution
for this corpus, suggesting that variation in reference-list length is not
a dominant source of influence asymmetry in economics and business publishing.

\textit{Census window $t^c$.}
Restricting the citation census to a single year ($t^c{=}2024$ only,
$N_S{=}1{,}466$) shifts $v$ substantially for many units but symmetrically
around the baseline in log space: sources gain or lose in roughly equal
measure, and the trend line does not systematically deviate from baseline.
This pattern reflects the fact that a one-year window introduces higher
sampling variance for every unit rather than a systematic upward or downward
bias, and is broadly consistent with the construction of the Clarivate AIS,
which also uses a one-year citation window.

\textit{Katz--Hubbell damping $\alpha$ and prior $\mu$.}
Setting $\alpha{=}0.85$ with either prior produces a large change below the
corpus-average line $v{=}1$, while units well above $v{=}1$ are largely
unaffected.
The mechanism is the prior $\mu$: at $\alpha < 1$ the stationary
distribution $\pi$ receives a contribution proportional to
$(1{-}\alpha)\mu_p$ at every unit, which establishes a floor on $\pi_p$.
When converted to influence $v_p \propto \pi_p/a_p$, units with
very small work counts $a_p$ are strongly lifted: the approximately 20 units
that stand well above the $\alpha{=}0.85$ cluster in the source panel each
have only 2--5 articles in the baseline corpus.
The choice of prior shape (uniform $1/N$ vs.\ unit-scaled $1/N_p$) shifts
the floor level but does not change the qualitative pattern.
For the baseline $\alpha{=}1$ the prior plays no role, and $v$ is determined
entirely by the network structure.